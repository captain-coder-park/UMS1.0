{% extends 'base.html.twig' %}

{% block body %}
    <h4 class="common_head4">Submit Reference</h4>
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <br>
                <div class="form-group pull-right">
                    <button type="submit" class="btn btn-primary form">Submit</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
